  <%= form_for color, {multipart: true} do |f| %>
    <div class="row">
      <%= f.hidden_field :product_id, value: "#{@product.id}" %>

      <div class="form-group col-4">
        <%= f.label :name %>
        <%= f.text_field :name, autofocus: true, class: 'form-control', id: "example" %>
      </div>

      <div class="form-group col-3">
        <%= f.label :hex_color %>
        <%= f.text_field :hex_color, class: 'form-control'  %>
      </div>

      <div class="form-group col-4">
        <%= f.label :color_sample %>
        <%= f.file_field :color_sample, class: 'form-control'  %>
      </div>

      <div class="actions col-1">
        <p></p>
        <%= f.submit "Add", class: 'btn btn-outline-secondary' %>
      </div>
    </div>
  <% end %>

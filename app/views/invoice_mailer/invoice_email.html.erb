<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>There is a new Order from <%= @order.shipping_first_name %> <%= @order.shipping_last_name %></h4>
    <p>
      The order totals <b><%= number_to_currency(@order.subtotal) %></b>, consisting of:
    </p>
    <p>
      Product Logo: <b><%= @order.product_logo_name %></b>
    </p>
    <br>
    <table>
      <tr>
        <th>Product</th>
        <th>Item Number<th>
        <th>Quantity</th>
        <th>Total Price</th>
      </tr>
        <% @order.order_items.find_each do |item| %>
      <tr>
        <td><%= item.product.name %></td>
        <td><%= item.product.item_number %></td>
        <td><%= item.quantity %></td>
        <td><%= number_to_currency(item.total_price) %></td>
      </tr>
        <% end %>
    </table>
  <br>
  <h4>Shipping Details</h4>
  <p><%= @order.shipping_first_name %> <%= @order.shipping_last_name %></p>
  <p><%= @order.shipping_address_line_1 %>, <%= @order.shipping_address_line_2 %></p>
  <p><%= @order.shipping_city %>, <%= @order.shipping_state %> <%= @order.shipping_zip_code %></p>
  <p><%= @order.shipping_phone_number %>, <%= @order.shipping_email %>
  <br>
  <h4>Have a great day!</h4>
  </body>
</html>
